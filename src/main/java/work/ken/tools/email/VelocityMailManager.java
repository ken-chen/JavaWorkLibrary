package work.ken.tools.email;


/*
 * Manager to send emails with velocity templates
 * 
 * 
 */
public interface VelocityMailManager {

	/*
	 * method to send email through Velocity
	 * 
	 * @param campaignFormBean - bean with data to send
	 * 
	 * @param campaignId - name of the campaign (passed as parameter)
	 */
	//public void sendVelocityTemplateEmail(CampaignFormBean campaignFormBean, String campaignId);
}
